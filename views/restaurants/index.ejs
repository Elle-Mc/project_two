<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title></title>
    </head>
        <!-- THE HEAD TAG PARTIAL -->
        <%- include("../partials/head") %>

    <body>
        <!-- THE HEADER TAG PARTIAL -->
        <%- include("../partials/header") %>
        <h1 class="title is-1">Where to Dine for Din</h1>
        <h3 class="subtitle is-3">Here's my list of restaurants to try</h3>
        <div class="container" style="display: flex; flex-wrap: wrap; justify-content: space-around;">
        <ul>
            <% for(let i = 0; i < restaurants.length; i++){ %>
                <li>
                    I want to go to 
                    <a href="/restaurants/<%=restaurants[i]._id%>">
                        <%=restaurants[i].name;%>.
                    </a>
                    It's located at 
                        <%=restaurants[i].location;%>.
                    A dish I'd like to try is
                        <%=restaurants[i].dish;%>.
                    <a href="/restaurants/<%=restaurants[i]._id %>/edit">Update</a>
                    <form action="/restaurants/<%=restaurants[i]._id %>?_method=DELETE" method="POST">
                        <input type="submit" value="DELETE"/>
                    </form><br />
                </li>
            <% } %>
        </ul>
    </div>
    <nav>
        <h3 class="subtitle is-3"><a href="/restaurants/new">Add a new restaurant</a></h3><br />
  </nav>
        <!-- THE HEAD TAG PARTIAL -->
        <%- include("../partials/footer") %>
    </body>

</html>
